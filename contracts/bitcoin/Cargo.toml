[package]
name = "aegis-bitcoin"
version = "2.0.0"
edition = "2021"
description = "Aegis Bitcoin Vault — Taproot 2-of-2 multisig with CSV owner recovery"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
